function greetUser(user, score) {
  return `Hello ${user}, your score is ${score}!`;
}

console.log(greetUser('Arjun', 95));

const note = `
This is a note.
It can span multiple lines
without using + operators.
`;
console.log(note);

const a = 5,
  b = 10;
console.log(`The sum of ${a} and ${b} is ${a + b}.`);
